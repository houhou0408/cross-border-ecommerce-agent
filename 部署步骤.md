# 部署步骤

> 目标：从零跑通跨境电商 AI Agent。支持"完整版(含 MySQL)"与"精简版(无数据库降级)"两种模式。

## 环境要求

- Python 3.10+
- pip（建议用虚拟环境 venv/conda）
- （可选）MySQL 8.0+
- （可选）网络可访问 LLM API（DeepSeek/通义千问/OpenAI 任一）

---

## 步骤 1：获取代码与创建虚拟环境

```bash
# 进入项目目录
cd 跨境AI助手          # 即 跨境电商AI助手 文件夹

# 创建虚拟环境
python -m venv venv
# Windows
venv\Scripts\activate
# macOS/Linux
source venv/bin/activate
```

## 步骤 2：安装依赖

```bash
pip install -r requirements.txt
```

> 若安装 `sentence-transformers` 较慢，可加国内镜像：
> `pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple`

## 步骤 3：配置环境变量

```bash
cp .env.example .env
```

编辑 `.env`，至少填写：
```ini
LLM_MODEL=deepseek-chat
LLM_API_KEY=sk-你的真实key
LLM_BASE_URL=https://api.deepseek.com/v1
```

> Embedding 默认用本地 BGE，无需 key。首次会自动下载模型（约100MB）。
> 国内下载慢可设环境变量：`set HF_ENDPOINT=https://hf-mirror.com`（Windows）
> 或 `export HF_ENDPOINT=https://hf-mirror.com`（macOS/Linux）

## 步骤 4：（可选）初始化 MySQL

仅当需要完整数据库功能时执行；跳过则工具自动降级到内置数据。

```bash
mysql -u root -p < 数据/数据库初始化.sql
```

确认 `.env` 中 MySQL 连接信息正确：
```ini
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=你的密码
MYSQL_DATABASE=cross_border_agent
```

## 步骤 5：构建向量知识库（首次必做）

```bash
python -c "from 模块.切片向量化 import build_vectorstore; build_vectorstore(force_rebuild=True)"
```

成功后会输出 `向量库构建完成，共 N 条向量`，并在 `向量库存储/` 目录生成持久化文件。

## 步骤 6：运行冒烟测试

```bash
python 测试/测试日志.py
```

测试日志输出到 `日志/测试日志.txt`。无 LLM key 时 Agent 端到端测试会 SKIP，其余模块应 PASS。

## 步骤 7：构建前端（React + Vite）

```bash
cd 前端/react-app
npm install
npm run build
cd ../..
```

构建产物输出到 `前端/dist/`，FastAPI 会自动挂载。若未构建，后端回退到旧版单文件前端。

## 步骤 8：启动演示

### 方式 A：Web 前端界面（推荐面试演示，最直观）

```bash
python 接口/FastAPI服务.py
# 或: uvicorn 接口.FastAPI服务:app --host 0.0.0.0 --port 8000
```

浏览器打开 **http://localhost:8000** 即可使用 Web 界面：
- 首次访问需注册/登录（用户名 ≥2 字符，密码 ≥6 位）
- 登录后进入主界面：顶部水平导航 8 个功能模块
- 智能对话：自然语言多任务编排，支持多轮对话记忆
- 知识库管理：上传/删除文档，实时入库
- 素材生成：图生视频/文生视频/卖点图生成
- 运行统计：调用量、耗时、幻觉拦截率、数据采集状态

接口文档访问：`http://localhost:8000/docs`

### 方式 B：命令行交互（备选，无需浏览器）

```bash
python 演示/命令行演示.py
```

进入交互后输入：
```
蓝牙音箱出口美国电子产品，货值500美元，查关税并换算成人民币
```
或用快捷指令 `/help` 查看全部指令。

调用示例（另开终端）：
```bash
# 健康检查
curl http://localhost:8000/health

# Agent 问答
curl -X POST http://localhost:8000/ask \
  -H "Content-Type: application/json" \
  -d "{\"query\": \"美国电子产品关税税率是多少\"}"

# 查关税
curl -X POST http://localhost:8000/tariff \
  -H "Content-Type: application/json" \
  -d "{\"country\": \"美国\", \"category\": \"电子产品\", \"value\": 500}"

# 运行统计
curl http://localhost:8000/stats
```

---

## 常见部署问题

| 问题 | 解决 |
|------|------|
| `ModuleNotFoundError: 模块` | 确保在项目根目录运行，入口已加 sys.path；勿在子目录运行 |
| Embedding 下载卡住 | 设 `HF_ENDPOINT=https://hf-mirror.com`，或改用 `EMBEDDING_PROVIDER=openai` |
| `AuthenticationError` LLM | 检查 `.env` 的 `LLM_API_KEY` 是否正确、余额是否充足 |
| MySQL 连接失败 | 不影响 Demo，工具自动降级；或检查 `.env` MySQL 配置与服务是否启动 |
| Chroma 报已存在 | 用 `build_vectorstore(force_rebuild=True)` 重建，或删除 `向量库存储/` 目录 |
| 中文乱码 | Windows 终端执行 `chcp 65001` 切 UTF-8 |

---

## 验收清单

- [ ] `python 测试/测试日志.py` 全部 PASS（或仅端到端 SKIP）
- [ ] 命令行 Demo 可完成"关税+汇率"复合任务并显示工具链
- [ ] `/listing 蓝牙音箱 amazon en` 能生成结构化 Listing
- [ ] 幻觉治理对低置信度回答追加风险提示
- [ ] FastAPI `/docs` 可正常调用各接口
- [ ] `日志/` 目录下生成运行日志文件
