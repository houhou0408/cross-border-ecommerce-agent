# 简历描述（可直接用于简历）

> 提供三个版本：精简版（1 条）、标准版（3-5 条）、面试口述版。按简历篇幅选用。

---

## 精简版（1 行项目描述）

**跨境电商 AI Agent 智能体** — 基于 LangChain + Chroma + RAG + ReAct/Function Call，构建跨境电商运营智能体，实现知识检索、关税/汇率工具调用、多任务编排与幻觉治理，含 FastAPI 接口与命令行演示。

---

## 标准版（推荐写入简历）

**项目名称：跨境电商 AI Agent 智能体**  |  Python · LangChain · Chroma · RAG · ReAct · Function Call · MySQL · FastAPI

- **RAG 知识库建设**：使用 LangChain 文档加载 + RecursiveCharacterTextSplitter 中文友好切片，以 BGE-small-zh 向量化并持久化至 Chroma；实现向量召回 + jieba 关键词重排的混合检索（融合权重 0.7:0.3），显著提升专有名词与 HS 编码的命中率。
- **Agent 多任务编排**：基于 `create_tool_calling_agent` 实现 ReAct + Function Call 双引擎，Agent 可自主串联知识检索、关税查询、汇率换算、Listing 生成等工具，单轮对话完成"查政策→算关税→换算币种"复合任务。
- **幻觉治理机制**：设计溯源校验器，通过关键词覆盖率、绝对化词惩罚、数字一致性核查给出置信度评分，低置信度自动追加风险提示并支持 LLM-as-Judge 二次校验，有效降低事实性幻觉。
- **工具与数据层**：以 `@tool` 装饰器封装关税(CIF 计算)、汇率、Listing 生成工具，对接 MySQL 做日志/存档/汇率关税快照，并实现"数据库不可用时降级内置数据"的容错策略，保证 Demo 全环境可运行。
- **工程化交付**：分层架构（加载/向量化/检索/调度/工具/日志），提供 FastAPI 接口（/ask /listing /tariff /currency /stats）与交互式命令行演示，配套冒烟测试、问题排查记录与检索/幻觉优化方案。

---

## 面试口述版（30 秒电梯陈述）

"我做了一个跨境电商 AI Agent，核心是用 LangChain 把 RAG 和 Function Call 结合起来。卖家一句自然语言进来，Agent 用 ReAct 思路自主决定调哪些工具——比如先检索平台政策知识库，再查关税、换算汇率、生成 Listing。技术上用 Chroma + BGE 做向量检索，加了关键词重排做混合检索提升召回；最特别的是做了一套幻觉治理，对答案做溯源校验和置信度评分，低置信度会自动标注风险提示。整体是分层架构，有 FastAPI 接口和命令行 Demo，可以直接演示。"

---

## 可量化亮点（按需补充真实数据）

- 知识库覆盖 3 大类（平台政策/选品/物流关税）× N 篇文档，切片后向量数 XX 条；
- 混合检索较纯向量检索，专有名词查询 Recall@4 提升约 XX%；
- 幻觉治理使事实性回答的 grounded 通过率从 XX% 提升至 XX%；
- 端到端平均延迟 < X 秒（含多工具串联）。

> 注：以上 XX 处请在本地实测后填入真实数据，更具说服力。
