# ===== 跨境电商 AI Agent 依赖 =====
# 版本对齐实际运行环境（本机 pip + libs/ vendored 包）。
# 注意：torch 在 Dockerfile 中单独以 CPU 版安装，此处不重复声明。

# LLM 编排（langchain 1.x 体系，经典 Agent 在 langchain-classic）
langchain==1.3.14
langchain-core==1.5.3
langchain-classic==1.0.8
langchain-community==0.4.2
langchain-openai==1.4.1
langchain-huggingface==1.2.2
langchain-chroma==1.1.0

# 向量库
chromadb==1.5.9

# Embedding 模型
sentence-transformers==3.3.1

# LLM 接口（OpenAI 兼容）
openai==2.52.0

# Web 服务
fastapi==0.115.6
uvicorn==0.34.0

# 数据库（Linux 容器用 PyPI 官方包；本机 libs/ 为 Windows 编译版）
mysql-connector-python==9.1.0
sqlalchemy==2.0.36

# 文档解析
pypdf==5.1.0

# 工具
pydantic==2.10.4
python-dotenv==1.0.1
numpy==2.2.6
jieba==0.42.1
requests==2.32.5

# FastAPI 文件上传（multipart 表单解析）
python-multipart==0.0.20

# 素材生成（DashScope 视频/图像，可选：未配置 Key 时自动降级）
dashscope==1.20.11
