# 优化方案：降低幻觉 & 优化检索效果

> 面试讲解重点文档。分"降低幻觉"与"优化检索"两大方向，给出当前已实现 + 可扩展方案。

---

## 一、降低幻觉（Hallucination Mitigation）

### 已实现
| 策略 | 实现位置 | 说明 |
|------|----------|------|
| RAG 溯源约束 | `模块/Agent调度.py` system prompt | 事实性内容强制先检索，禁止凭记忆编造 |
| 工具优先 | system prompt + Function Call | 数字类(税率/汇率/金额)必须走工具，不得估算 |
| 检索阈值过滤 | `模块/检索器.py` score_threshold | 低质上下文不进 Prompt，避免误导 |
| 置信度评分 | `模块/幻觉治理.py` verify | 关键词覆盖+绝对化词惩罚+数字核查综合打分 |
| 兜底标注 | `annotate_answer` | 低置信度自动追加"未在知识库核实"风险提示 |
| 低温度生成 | `config.py` temperature=0.2 | 降低采样随机性 |
| 长度上限 | HALLUCINATION_CONFIG max_answer_tokens | 防止发散式长篇 |

### 可扩展
1. **Self-Check / Self-Consistency**：让 LLM 对同一问题生成多版答案，取一致部分；
2. **LLM-as-Judge**：`llm_judge` 已预留接口，开启后用低温度模型做细粒度事实核查；
3. **引用强制**：要求答案每条结论标注 [来源编号]，无来源的结论直接剔除；
4. **知识库时效性标注**：文档元数据加 `updated_at`，过期内容降权；
5. **多轮记忆治理**：对话历史过长时做摘要压缩，避免历史幻觉累积。

---

## 二、优化检索效果（Retrieval Optimization）

### 已实现
| 策略 | 实现位置 | 说明 |
|------|----------|------|
| 中文友好切片 | `模块/切片向量化.py` | 按 句号/换行/分号 递归切分，保留语义完整 |
| BGE 中文 Embedding | `config.py` | `bge-small-zh-v1.5`，归一化向量便于余弦相似 |
| 混合检索 | `模块/检索器.py` | 向量召回 + jieba 关键词重排(0.7:0.3) |
| 召回扩充再截断 | top_k*2 召回 | 先多召回，重排后取 top_k，提升 Precision |
| 相似度阈值 | score_threshold | 低于阈值视为未命中，避免噪声 |

### 可扩展
1. **Query 改写 / 扩展**：用 LLM 把口语化 query 改写为多路检索子查询（HyDE / Multi-Query）；
2. **Parent-Child 切片**：检索小块命中后回溯到所在大块送入 LLM，兼顾命中精度与上下文完整；
3. **MMR 多样性重排**：在重排阶段加入 Maximal Marginal Relevance，去重冗余结果；
4. **Cross-Encoder 重排**：召回后用 bge-reranker 等做精排，进一步提升 Top-1 准确率；
5. **元数据过滤**：按平台/目的国/类目等元数据预过滤，缩小检索范围；
6. **向量化增量更新**：知识库新增文档时增量 embedding，而非全量重建。

---

## 三、效果度量指标（建议补充）

| 指标 | 含义 | 目标 |
|------|------|------|
| Recall@k | 前 k 条是否含正确答案 | ≥ 0.9 |
| 幻觉拦截率 | 被标注低置信度的比例 | 监控异常突增 |
| 平均置信度 | grounding_score 均值 | ≥ 0.6 |
| 端到端延迟 | orchestrate 耗时 | < 8s |
| 工具命中率 | 应调工具且实际调用比例 | ≥ 0.95 |

度量数据可通过 `模块/日志统计.py` 的 `stats()` 与 `dump_buffer()` 采集，导出后做离线分析。
